# FastAPI and server
fastapi>=0.125.0
uvicorn[standard]>=0.38.0
python-multipart>=0.0.20
starlette>=0.49.0

# Database - SQLite (built-in, no extra package needed)
sqlalchemy>=2.0.45
alembic>=1.16.0

# Authentication
python-jose[cryptography]>=3.5.0
bcrypt>=5.0.0
cryptography>=46.0.0

# Pydantic
pydantic>=2.11.0
pydantic-settings>=2.11.0
email-validator>=2.3.0

# AI - OpenAI
openai>=2.13.0

# PDF Processing
pypdf>=6.4.0
pdf2image>=1.17.0
pillow>=11.3.0
reportlab>=4.4.0
PyMuPDF>=1.26.0           # Fast PDF rendering (fitz) - much faster than pdf2image
pytesseract>=0.3.13       # OCR wrapper for Tesseract

# Note: Tesseract-OCR must be installed on the system:
# macOS: brew install tesseract
# Ubuntu: sudo apt-get install tesseract-ocr
# Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki

# Excel Export
openpyxl>=3.1.5

# HTTP Client
httpx>=0.28.0

# Utilities
python-dotenv>=1.2.0
aiofiles>=25.1.0
tenacity>=9.1.0           # Retry logic for LLM extraction

# Testing (optional - for development)
pytest>=8.4.0
pytest-mock>=3.15.0

# Note: The following are Python standard library modules used (no install needed):
# - json, re, logging, asyncio, time, datetime, typing, dataclasses
# - collections, statistics, difflib, enum, uuid, contextlib, io, os, hashlib, tempfile
# - concurrent.futures, functools
